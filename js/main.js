import { btnletters, hiddenWordDisplay, btnClick } from "./WITP.js";

document.addEventListener("DOMContentLoaded", (e) => {
  // preLoader();
  // storage();
  btnletters();
  hiddenWordDisplay();
  btnClick();
});
